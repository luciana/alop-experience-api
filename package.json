{
  "name": "ALOP-Adapter-API",
  "version": "1.0.0",
  "description": "Experience API for ALOP - it transforms and orchestrates API calls to business API to provide the necessary experience data for the mobile UI apps.",
  "scripts": {
    "test": "NODE_ENV=test mocha './src/app/test/**/*.js' --exit",
    "start": "NODE_ENV=production pm2 start server.js  --watch --no-daemon",
    "start-dev": "NODE_ENV=development pm2 start server.js  --watch --no-daemon",
    "stop": "pm2 kill"
  },
  "author": "luciana",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.18.2",
    "compression": "^1.7.2",
    "config": "^1.30.0",
    "dotenv": "^5.0.1",
    "express": "^4.15.3",
    "pm2": "^2.10.2",
    "redis": "^2.8.0",
    "request": "^2.79.0",
    "request-promise": "^4.1.1",
    "rxjs": "^5.5.7",
    "uuid": "^3.2.1"
  },
  "engines": {
    "node": ">=8"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "mocha": "^5.2.0",
    "supertest": "^3.1.0"
  }
}
